# 🚨 راهنمای سریع رفع مشکل دیتابیس

## ❌ مشکل فعلی
خطای `ERROR: 42703: column "rating" does not exist` در اجرای فایل SQL

## ✅ راه‌حل سریع

### مرحله 1: استفاده از فایل ساده
فایل `database/create_tables_minimal.sql` را در Supabase اجرا کنید:

1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. روی "SQL Editor" کلیک کنید
4. محتوای فایل `database/create_tables_minimal.sql` را کپی کنید
5. روی "Run" کلیک کنید

### مرحله 2: بررسی ایجاد جداول
پس از اجرا، باید پیام زیر را ببینید:
```
Minimal tables created successfully!
```

### مرحله 3: تست سیستم
1. سرور را restart کنید
2. به صفحه `/complete-profile` بروید
3. خطاها باید برطرف شده باشند

## 🔧 فایل‌های SQL موجود

### 1. `database/create_tables_minimal.sql` ✅ **توصیه شده**
- جداول حداقلی
- بدون ویژگی‌های پیچیده
- مناسب برای تست سریع

### 2. `database/create_tables_simple.sql` ⚠️ **متوسط**
- جداول کامل‌تر
- بدون RLS پیچیده
- ممکن است خطا داشته باشد

### 3. `database/create_tables.sql` ❌ **مشکل دارد**
- جداول کامل با RLS
- دارای خطا در index ها
- نیاز به اصلاح دارد

## 🚀 در صورت موفقیت

اگر جداول با موفقیت ایجاد شدند:
- ✅ صفحه `/complete-profile` کار می‌کند
- ✅ فرم‌های ثبت‌نام کار می‌کنند
- ✅ سیستم آماده استفاده است

## 📞 در صورت مشکل

### خطاهای رایج:
1. **"relation already exists"**
   - جدول قبلاً وجود دارد، نادیده بگیرید

2. **"permission denied"**
   - مطمئن شوید که در Supabase هستید

3. **"syntax error"**
   - فایل را دوباره کپی کنید

### تماس با پشتیبانی:
- Console مرورگر را بررسی کنید
- Supabase logs را چک کنید
- Error message دقیق را یادداشت کنید

## 🎯 نتیجه نهایی

پس از اجرای این مراحل:
- ✅ جداول دیتابیس ایجاد شده‌اند
- ✅ سیستم ثبت‌نام کار می‌کند
- ✅ صفحه complete-profile بدون خطا کار می‌کند
