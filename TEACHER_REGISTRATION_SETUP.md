# راهنمای راه‌اندازی سیستم ثبت‌نام چند مرحله‌ای معلمان

## خلاصه
این سیستم یک فرم ثبت‌نام چند مرحله‌ای برای معلمان است که شامل تمام اطلاعات مورد نیاز برای پلتفرم‌های آموزش زبان مانند Preply می‌باشد.

## ویژگی‌های کلیدی

### ✅ 5 مرحله ثبت‌نام
1. **اطلاعات پایه**: نام، ایمیل، رمز عبور، تلفن، جنسیت، تاریخ تولد، کد ملی
2. **پروفایل تدریس**: زبان‌ها، سطح‌ها، نوع کلاس، تجربه، تحصیلات، بیوگرافی
3. **زمان‌بندی و قیمت‌گذاری**: روزهای کاری، ساعات کاری، نرخ ساعتی، حداکثر دانش‌آموز
4. **مدارک و روش‌ها**: روش‌های تدریس، گواهینامه‌ها، دستاوردها
5. **تایید نهایی**: آدرس، توضیحات، موافقت با قوانین

### ✅ زبان‌های پشتیبانی شده
- فارسی 🇮🇷
- انگلیسی 🇺🇸
- عربی 🇸🇦
- فرانسه 🇫🇷
- آلمانی 🇩🇪
- اسپانیایی 🇪🇸
- ایتالیایی 🇮🇹
- روسی 🇷🇺
- چینی 🇨🇳
- ژاپنی 🇯🇵
- کره‌ای 🇰🇷
- ترکی 🇹🇷

### ✅ سطح‌های تدریس
- مبتدی
- متوسط
- پیشرفته
- آکادمیک
- آیلتس
- تافل
- مکالمه
- گرامر
- تجاری
- کودکان

### ✅ نوع کلاس‌ها
- آنلاین
- حضوری
- ترکیبی

### ✅ روش‌های تدریس
- روش ارتباطی
- ترجمه گرامری
- روش مستقیم
- شنیداری-گفتاری
- مبتنی بر وظیفه
- مبتنی بر محتوا
- ترکیبی

## نصب و راه‌اندازی

### 1. اجرای اسکریپت دیتابیس
فایل `database/teachers_table_setup.sql` را در Supabase SQL Editor اجرا کنید:

```sql
-- کپی کردن محتوای فایل و اجرا در Supabase
```

### 2. بررسی ساختار فایل‌ها
```
app/register/teacher/page.tsx  ✅ فرم چند مرحله‌ای
database/teachers_table_setup.sql  ✅ ساختار دیتابیس
app/register/page.tsx  ✅ لینک به فرم معلم
```

### 3. تست سیستم
1. به `/register?type=teacher` بروید
2. روی دکمه "ثبت‌نام کامل معلم" کلیک کنید
3. فرم 5 مرحله‌ای را تکمیل کنید

## ساختار دیتابیس

### جدول `teachers`
```sql
CREATE TABLE teachers (
  -- اطلاعات پایه
  id UUID PRIMARY KEY,
  email TEXT UNIQUE,
  first_name TEXT,
  last_name TEXT,
  phone TEXT,
  gender TEXT,
  birthdate DATE,
  national_id TEXT UNIQUE,
  
  -- پروفایل تدریس
  languages TEXT[],
  levels TEXT[],
  class_types TEXT[],
  experience_years INTEGER,
  education TEXT,
  bio TEXT,
  
  -- زمان‌بندی و قیمت
  available_days TEXT[],
  available_hours TEXT[],
  hourly_rate INTEGER,
  max_students_per_class INTEGER,
  location TEXT,
  
  -- مدارک و روش‌ها
  certificates TEXT[],
  teaching_methods TEXT[],
  achievements TEXT[],
  
  -- وضعیت
  status TEXT DEFAULT 'pending',
  rating DECIMAL(3,2),
  total_students INTEGER,
  
  -- متادیتا
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);
```

### Index های مهم
- `idx_teachers_languages` - جستجو در زبان‌ها
- `idx_teachers_levels` - جستجو در سطح‌ها
- `idx_teachers_location` - جستجو در مکان
- `idx_teachers_hourly_rate` - فیلتر قیمت
- `idx_teachers_search_vector` - جستجوی متنی

## API Functions

### 1. `get_active_teachers()`
دریافت لیست معلمان فعال

### 2. `search_teachers(query)`
جستجو در معلمان با متن

### 3. `filter_teachers(languages, levels, max_rate, location)`
فیلتر کردن معلمان بر اساس معیارها

### 4. `is_teacher_active(teacher_id)`
بررسی فعال بودن معلم

## امنیت و دسترسی

### RLS Policies
- معلمان فقط پروفایل خود را می‌بینند
- همه کاربران معلمان فعال را می‌بینند
- فقط ادمین‌ها وضعیت معلمان را تغییر می‌دهند

### Validation
- اعتبارسنجی هر مرحله قبل از ادامه
- بررسی فیلدهای اجباری
- اعتبارسنجی ایمیل و رمز عبور

## ویژگی‌های پیشرفته

### 1. جستجوی متنی
- استفاده از PostgreSQL Full-Text Search
- وزن‌دهی به فیلدهای مختلف
- پشتیبانی از زبان فارسی

### 2. انیمیشن‌ها
- استفاده از Framer Motion
- انتقال نرم بین مراحل
- انیمیشن‌های ورود و خروج

### 3. Responsive Design
- طراحی موبایل-فرست
- Grid های انعطاف‌پذیر
- چک‌باکس‌های بهینه‌شده

## سفارشی‌سازی

### اضافه کردن زبان جدید
```typescript
const TEACHING_LANGUAGES = [
  // ... زبان‌های موجود
  { value: 'new_language', label: 'نام زبان', flag: '🏳️' }
];
```

### اضافه کردن سطح جدید
```typescript
const TEACHING_LEVELS = [
  // ... سطح‌های موجود
  { value: 'new_level', label: 'نام سطح' }
];
```

### تغییر تعداد مراحل
1. تغییر `currentStep` در state
2. اضافه کردن validation جدید
3. ایجاد render function جدید

## عیب‌یابی

### مشکلات رایج

#### 1. خطای "languages field not found"
- مطمئن شوید جدول `teachers` ایجاد شده
- اسکریپت SQL را دوباره اجرا کنید

#### 2. خطای "auth.users not found"
- Supabase Auth را فعال کنید
- RLS policies را بررسی کنید

#### 3. فرم کار نمی‌کند
- Console مرورگر را بررسی کنید
- Network tab را چک کنید
- Supabase logs را بررسی کنید

### Log ها
```typescript
console.log('Form data:', formData);
console.log('Current step:', currentStep);
console.log('Validation result:', validateStep(currentStep));
```

## تست و کیفیت

### تست‌های ضروری
1. ✅ تکمیل تمام مراحل
2. ✅ validation هر مرحله
3. ✅ ذخیره در دیتابیس
4. ✅ redirect پس از ثبت‌نام
5. ✅ نمایش خطاها

### تست‌های اضافی
1. ✅ Responsive design
2. ✅ Accessibility
3. ✅ Performance
4. ✅ Security

## بهینه‌سازی

### 1. Lazy Loading
```typescript
const TeacherRegistrationForm = lazy(() => import('./TeacherRegistrationForm'));
```

### 2. Debouncing
```typescript
const debouncedUpdate = useMemo(
  () => debounce(updateFormData, 300),
  []
);
```

### 3. Memoization
```typescript
const memoizedValidation = useMemo(
  () => validateStep(currentStep),
  [currentStep, formData]
);
```

## پشتیبانی و نگهداری

### 1. به‌روزرسانی‌های منظم
- بررسی Supabase updates
- به‌روزرسانی dependencies
- تست compatibility

### 2. Monitoring
- Error tracking
- Performance monitoring
- User analytics

### 3. Backup
- Backup دیتابیس
- Version control
- Documentation updates

## نتیجه‌گیری

این سیستم ثبت‌نام چند مرحله‌ای معلمان یک راه‌حل کامل و حرفه‌ای برای پلتفرم‌های آموزش زبان است که شامل:

- ✅ فرم جامع و کاربرپسند
- ✅ امنیت بالا
- ✅ عملکرد بهینه
- ✅ قابلیت گسترش
- ✅ مستندات کامل

برای شروع، اسکریپت دیتابیس را اجرا کرده و فرم را تست کنید.
