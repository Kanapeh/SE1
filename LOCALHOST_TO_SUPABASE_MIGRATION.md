# 🔄 راهنمای مهاجرت از Localhost به Supabase

## 🎯 مشکل
مقالات در localhost نمایش داده می‌شوند اما در production نه. این یعنی مقالات در دیتابیس محلی ذخیره شده‌اند نه در Supabase.

## 🔍 تشخیص مشکل

### مرحله 1: بررسی محیط localhost
1. به این آدرس بروید: `http://localhost:3000/debug-localhost-database`
2. نتایج را بررسی کنید
3. اگر "Local Database" نمایش داده شد، مشکل پیدا شد!

### مرحله 2: بررسی Environment Variables
مطمئن شوید که فایل `.env.local` وجود دارد و شامل این متغیرها است:

```bash
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

## 🚀 راه‌حل کامل

### مرحله 1: ایجاد فایل Environment
1. فایل `.env.local` در root پروژه ایجاد کنید
2. متغیرهای Supabase را اضافه کنید:

```bash
# .env.local
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

### مرحله 2: دریافت کلیدهای Supabase
1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. به Settings > API بروید
4. Project URL و anon public key را کپی کنید

### مرحله 3: ایجاد جدول در Supabase
1. به Supabase SQL Editor بروید
2. اسکریپت زیر را اجرا کنید:

```sql
-- محتوای فایل database/create_blog_posts_table.sql
-- (که قبلاً ایجاد کردیم)
```

### مرحله 4: مهاجرت مقالات
1. سرور را restart کنید: `npm run dev`
2. به این آدرس بروید: `http://localhost:3000/api/migrate-posts-to-supabase`
3. درخواست POST ارسال کنید (از Postman یا curl استفاده کنید)

### مرحله 5: تست
1. به `http://localhost:3000/debug-localhost-database` بروید
2. مطمئن شوید که "Supabase Cloud" نمایش داده می‌شود
3. مقالات را در `http://localhost:3000/blog` بررسی کنید

## 🔧 اسکریپت‌های کمکی

### اسکریپت curl برای مهاجرت
```bash
curl -X POST http://localhost:3000/api/migrate-posts-to-supabase
```

### اسکریپت بررسی دیتابیس
```sql
-- بررسی مقالات در Supabase
SELECT id, title, slug, status, created_at 
FROM blog_posts 
ORDER BY created_at DESC;
```

## 📋 چک‌لیست کامل

### قبل از شروع:
- [ ] فایل `.env.local` ایجاد شده
- [ ] متغیرهای Supabase اضافه شده
- [ ] کلیدهای Supabase از Dashboard دریافت شده

### مراحل مهاجرت:
- [ ] جدول `blog_posts` در Supabase ایجاد شده
- [ ] سرور restart شده
- [ ] API مهاجرت اجرا شده
- [ ] مقالات در Supabase بررسی شده

### تست نهایی:
- [ ] مقالات در localhost نمایش داده می‌شوند
- [ ] مقالات در production نمایش داده می‌شوند
- [ ] Environment variables درست تنظیم شده‌اند

## 🐛 عیب‌یابی

### مشکل: "Table does not exist"
**راه‌حل:** جدول `blog_posts` را در Supabase ایجاد کنید

### مشکل: "Environment variables not set"
**راه‌حل:** فایل `.env.local` را ایجاد و متغیرها را اضافه کنید

### مشکل: "Posts not showing in production"
**راه‌حل:** Environment variables را در Vercel تنظیم کنید

## 🎉 نتیجه

بعد از انجام این مراحل:
- ✅ مقالات در localhost از Supabase خوانده می‌شوند
- ✅ مقالات در production نمایش داده می‌شوند
- ✅ دیتابیس یکپارچه است
- ✅ مشکل حل شده است

## 📞 در صورت مشکل

اگر بعد از انجام این مراحل مشکل ادامه داشت:

1. **بررسی Console**: خطاهای JavaScript را بررسی کنید
2. **بررسی Network**: درخواست‌های HTTP را بررسی کنید
3. **بررسی Supabase Logs**: در Supabase Dashboard بررسی کنید
4. **بررسی Environment**: متغیرهای محیطی را دوباره چک کنید

---

**نکته مهم**: همیشه قبل از تغییرات مهم، از دیتابیس خود پشتیبان تهیه کنید.
