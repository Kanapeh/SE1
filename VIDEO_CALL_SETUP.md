# 🎥 راهنمای تنظیم تماس تصویری

## ❌ مشکل: دوربین کار نمی‌کند

### 🔍 علت اصلی:
تماس تصویری نیاز به **HTTPS** دارد، ولی سایت شما روی **HTTP** اجرا می‌شود.

---

## ✅ راه‌حل‌های آسان:

### 🚀 راه‌حل 1: استفاده از ngrok (توصیه شده)

```bash
# نصب ngrok (یکبار)
npm install -g ngrok

# اجرای سرور با HTTPS
npm run dev:https
```

این کامند:
- سرور Next.js را روی port 3000 اجرا می‌کند
- ngrok tunnel HTTPS ایجاد می‌کند  
- آدرس HTTPS می‌دهد (مثل: `https://abc123.ngrok.io`)

### 🌐 راه‌حل 2: localhost (محدود)

```bash
# اجرای عادی
npm run dev

# سپس استفاده از:
http://localhost:3000
```

**نکته:** فقط روی همین کامپیوتر کار می‌کند

### 📱 راه‌حل 3: IP محلی (شبکه محلی)

```bash
# پیدا کردن IP محلی
ifconfig | grep "inet " | grep -v 127.0.0.1

# استفاده از IP (مثال):
http://192.168.1.100:3000
```

**نکته:** فقط در همان شبکه WiFi کار می‌کند

---

## 🔧 تست کردن:

1. **اجرای سرور:**
   ```bash
   npm run dev:https
   ```

2. **باز کردن آدرس HTTPS در مرورگر**

3. **رفتن به صفحه تماس تصویری:**
   ```
   /students/temp-user-id/video-call
   ```

4. **اجازه دادن به دوربین:** 
   - روی "Allow" کلیک کنید
   - یا آیکون دوربین در address bar

---

## 🛠️ رفع مشکل:

### اگر ngrok کار نکرد:
```bash
# نصب دوباره
npm uninstall -g ngrok
npm install -g ngrok

# یا استفاده از npx
npx ngrok http 3000
```

### اگر دوربین باز هم کار نکرد:
1. مطمئن شوید آدرس HTTPS است (`https://`)
2. F5 کنید (refresh)
3. مرورگر را restart کنید
4. دکمه "راهنما" در صفحه تماس کلیک کنید

### مرورگرهای پشتیبانی شده:
- ✅ Chrome
- ✅ Firefox  
- ✅ Safari
- ✅ Edge

---

## 📋 Commands مفید:

```bash
# اجرای عادی
npm run dev

# اجرای با HTTPS (ngrok)
npm run dev:https

# نصب ngrok
npm install -g ngrok

# تست دستی ngrok
npx ngrok http 3000
```

---

## 🔐 نکات امنیتی:

- آدرس ngrok عمومی است (همه می‌توانند دسترسی داشته باشند)
- برای production، SSL certificate درست تهیه کنید
- localhost فقط برای development امن است

---

## 📞 پشتیبانی:

اگر مشکل ادامه دارد:
1. در صفحه تماس، دکمه "راهنما" کلیک کنید
2. مراحل troubleshooting را دنبال کنید  
3. مرورگر و تنظیمات را بررسی کنید
