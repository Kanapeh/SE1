# 🚀 راهنمای سریع فعال‌سازی Google OAuth

## 🚨 مشکل: "Unsupported provider: provider is not enabled"

### مرحله 1: ورود به Supabase Dashboard
1. به [supabase.com](https://supabase.com) بروید
2. پروژه خود را انتخاب کنید

### مرحله 2: فعال‌سازی Google Provider
1. از منوی سمت چپ روی **"Authentication"** کلیک کنید
2. روی **"Providers"** کلیک کنید
3. **"Google"** را پیدا کنید
4. روی **toggle switch** کلیک کنید تا فعال شود (سبز شود)

### مرحله 3: تنظیم Credentials
1. **Client ID** را از Google Cloud Console کپی کنید
2. **Client Secret** را از Google Cloud Console کپی کنید
3. روی **"Save"** کلیک کنید

## 📸 مراحل تصویری:

```
Supabase Dashboard
├── Authentication (منوی سمت چپ)
│   └── Providers
│       └── Google
│           ├── [ ] Enable (قبل از فعال‌سازی)
│           ├── [✓] Enable (بعد از فعال‌سازی)
│           ├── Client ID: [your-client-id]
│           ├── Client Secret: [your-client-secret]
│           └── [Save] Button
```

## 🔍 بررسی وضعیت:

اگر Google OAuth فعال است، باید این موارد را ببینید:
- ✅ Toggle switch سبز باشد
- ✅ Client ID و Secret پر شده باشند
- ✅ پیام "Provider enabled" نمایش داده شود

## 🧪 تست:

1. سرور را restart کنید:
   ```bash
   npm run dev
   ```

2. به `/register` بروید
3. دکمه "ادامه با گوگل" را کلیک کنید
4. باید به صفحه گوگل منتقل شوید

## ❌ اگر هنوز کار نمی‌کند:

1. **Console را چک کنید** (F12)
2. **Supabase logs** را بررسی کنید
3. **Google Cloud Console** را چک کنید
4. **Client ID و Secret** را دوباره وارد کنید

---

**نکته**: این مشکل معمولاً با فعال‌سازی Google provider در Supabase حل می‌شود.
