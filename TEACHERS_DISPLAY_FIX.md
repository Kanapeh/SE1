# راهنمای حل مشکل عدم نمایش تمام معلم‌ها در داشبورد ادمین

## 🔍 مشکل
دو معلم ثبت‌نام کرده‌اند اما فقط یک معلم در `/admin/teachers` نمایش داده می‌شود.

## 🔧 راه‌حل

### مرحله 1: بررسی وضعیت فعلی

#### 1.1. اجرای اسکریپت بررسی
```sql
-- در Supabase SQL Editor اجرا کنید:
-- فایل: database/check_teachers.sql
```

این اسکریپت موارد زیر را بررسی می‌کند:
- تعداد کل معلمان
- وضعیت معلمان (pending, approved, active, rejected)
- جزئیات هر معلم
- RLS policies
- دسترسی ادمین

### مرحله 2: رفع مشکل RLS Policies

#### 2.1. اجرای اسکریپت اصلاح
```sql
-- در Supabase SQL Editor اجرا کنید:
-- فایل: database/fix_teachers_display.sql
```

این اسکریپت:
- تمام policies قدیمی را حذف می‌کند
- policies جدید را ایجاد می‌کند
- دسترسی ادمین را تنظیم می‌کند
- تست دسترسی انجام می‌دهد

### مرحله 3: تست در داشبورد

#### 3.1. بررسی کنسول مرورگر
1. F12 را فشار دهید
2. به تب Console بروید
3. خطاهای مربوط به Supabase را بررسی کنید

#### 3.2. بررسی Network
1. F12 را فشار دهید
2. به تب Network بروید
3. درخواست‌های API را بررسی کنید

#### 3.3. تست دسترسی
1. به `/admin/teachers` بروید
2. صفحه را refresh کنید
3. باید تمام معلمان نمایش داده شوند

## 🚨 مشکلات احتمالی

### مشکل 1: RLS Policy مشکل دارد
**علت:** Policy های قدیمی مانع دسترسی می‌شوند
**راه‌حل:** 
1. فایل `fix_teachers_display.sql` را اجرا کنید
2. policies جدید ایجاد می‌شوند

### مشکل 2: کاربر ادمین نیست
**علت:** کاربر در جدول admins ثبت نشده است
**راه‌حل:**
1. فایل `fix_teachers_display.sql` را اجرا کنید
2. کاربر به عنوان ادمین اضافه می‌شود

### مشکل 3: مشکل در دیتابیس
**علت:** جدول teachers یا admins مشکل دارد
**راه‌حل:**
1. فایل `check_teachers.sql` را اجرا کنید
2. وضعیت دیتابیس را بررسی کنید

## 📋 چک‌لیست

- [ ] فایل `check_teachers.sql` اجرا شد
- [ ] فایل `fix_teachers_display.sql` اجرا شد
- [ ] کاربر در جدول admins ثبت شد
- [ ] RLS policies درست تنظیم شدند
- [ ] دسترسی به جدول teachers تایید شد
- [ ] صفحه `/admin/teachers` refresh شد
- [ ] تمام معلمان نمایش داده شدند

## 🔍 دیباگ

### بررسی لاگ‌های کنسول
1. F12 را فشار دهید
2. به تب Console بروید
3. خطاهای مربوط به Supabase را بررسی کنید

### بررسی Network
1. F12 را فشار دهید
2. به تب Network بروید
3. درخواست‌های API را بررسی کنید

### بررسی Supabase Logs
1. به Supabase Dashboard بروید
2. به بخش Logs بروید
3. خطاهای مربوط به دیتابیس را بررسی کنید

## ✅ تست نهایی

بعد از اجرای تمام مراحل:
1. به `/admin/teachers` بروید
2. باید تمام معلمان (شامل هر دو معلم) نمایش داده شوند
3. باید بتوانید وضعیت آن‌ها را تغییر دهید
4. آمار معلمان در بالای صفحه باید درست باشد

## 📞 پشتیبانی

اگر مشکل حل نشد:
1. نتایج اسکریپت `check_teachers.sql` را بررسی کنید
2. لاگ‌های کنسول را بررسی کنید
3. پیام‌های خطا را کپی کنید
4. با تیم پشتیبانی تماس بگیرید

## 🔄 تغییرات انجام شده

1. **ایجاد اسکریپت بررسی:** `database/check_teachers.sql`
2. **ایجاد اسکریپت اصلاح:** `database/fix_teachers_display.sql`
3. **رفع RLS policies:** policies جدید برای دسترسی ادمین
4. **تنظیم دسترسی ادمین:** کاربر به عنوان ادمین اضافه می‌شود
