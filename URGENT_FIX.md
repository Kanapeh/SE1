# 🚨 حل فوری مشکل دوربین

## ❌ مشکل شما:
شما از IP استفاده می‌کنید: `http://172.20.10.10:3000`
**این آدرس باعث مشکل می‌شود!**

## ✅ حل فوری:

### مرحله ۱: آدرس درست استفاده کنید
```bash
# ❌ اشتباه (چیزی که الان استفاده می‌کنید)
http://172.20.10.10:3000/students/temp-user-id/video-call

# ✅ درست (باید از این استفاده کنید)  
http://localhost:3000/students/temp-user-id/video-call
```

### مرحله ۲: مستقیم تست کنید
```bash
# در مرورگر این آدرس را باز کنید:
http://localhost:3000/test-video
```

---

## 🎯 چرا این مشکل پیش آمد؟

### علت اصلی:
- مرورگرها فقط در **localhost** یا **HTTPS** اجازه دسترسی دوربین می‌دهند
- `172.20.10.10` (IP محلی) **مجاز نیست** برای getUserMedia
- **localhost امن محسوب می‌شود**

### راه‌حل:
- از `localhost` استفاده کنید
- یا از HTTPS tunnel

---

## 🔧 تست سریع:

### ۱. باز کردن localhost:
```
http://localhost:3000/test-video
```

### ۲. کلیک "تست به عنوان دانش‌آموز"

### ۳. باید خودتان را ببینید!

---

## 📱 اگر روی گوشی تست می‌کنید:

### برای گوشی HTTPS لازم است:
```bash
# نصب localtunnel
npm install -g localtunnel

# Terminal 1: سرور  
npm run dev

# Terminal 2: tunnel
npx localtunnel --port 3000
```

سپس از آدرس HTTPS که localtunnel می‌دهد استفاده کنید.

---

## ✨ نتیجه:

**localhost = دوربین کار می‌کند ✅**
**IP محلی = دوربین کار نمی‌کند ❌**
