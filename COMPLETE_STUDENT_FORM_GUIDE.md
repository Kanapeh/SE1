# 🎯 فرم دانش‌آموز کامل شده

## ✅ **فرم گسترش یافته با اطلاعات ضروری:**

### **📋 فیلدهای فرم:**

#### **1. اطلاعات شخصی:**
- **نام** (`first_name`) - اجباری
- **نام خانوادگی** (`last_name`) - اجباری  
- **شماره تلفن** (`phone`) - اجباری
- **سن** (`age`) - اجباری (5-100 سال)

#### **2. اطلاعات زبان:**
- **سطح زبان فعلی** (`current_language_level`) - اجباری
  - مبتدی، ابتدایی، متوسط، متوسط پیشرفته، پیشرفته
- **زبان‌های انتخابی** (`preferred_languages`) - اجباری
  - فقط آلمانی و انگلیسی

#### **3. اطلاعات یادگیری:**
- **اهداف یادگیری** (`learning_goals`) - اختیاری
  - مثال: سفر، کار، تحصیل، علاقه شخصی
- **سبک یادگیری** (`preferred_learning_style`) - اختیاری
  - تصویری، شنیداری، خواندن و نوشتن، تعاملی، ترکیبی

## 🔧 **فایل‌های تغییر یافته:**

### **1. `app/complete-profile/page.tsx`:**
```typescript
interface StudentProfile {
  id: string;
  email: string;
  first_name: string;           // نام
  last_name: string;            // نام خانوادگی
  phone: string;                // شماره تلفن
  age: number;                  // سن
  current_language_level: string;  // سطح زبان فعلی
  preferred_languages: string[];    // زبان‌های انتخابی
  learning_goals: string;       // اهداف یادگیری
  preferred_learning_style: string; // سبک یادگیری
}
```

### **2. `database/create_students_table.sql`:**
```sql
CREATE TABLE public.students (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  first_name TEXT NOT NULL,              -- نام
  last_name TEXT NOT NULL,               -- نام خانوادگی
  phone TEXT NOT NULL,                   -- شماره تلفن
  age INTEGER NOT NULL,                  -- سن
  current_language_level TEXT NOT NULL,  -- سطح زبان فعلی
  preferred_languages TEXT[] NOT NULL,    -- زبان‌های انتخابی
  learning_goals TEXT,                   -- اهداف یادگیری
  preferred_learning_style TEXT,         -- سبک یادگیری
  status TEXT DEFAULT 'active',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## 🚀 **مراحل اجرا:**

### **مرحله 1: حذف جدول قدیمی**
```sql
DROP TABLE IF EXISTS public.students;
```

### **مرحله 2: ایجاد جدول جدید**
فایل `database/create_students_table.sql` را در Supabase اجرا کنید

### **مرحله 3: تست فرم**
1. سرور را restart کنید
2. به `/complete-profile?type=student` بروید
3. فرم کامل شده را تست کنید

## 🎯 **مزایای فرم کامل شده:**

- ✅ **اطلاعات کامل**: تمام اطلاعات ضروری برای پروفایل
- ✅ **شخصی‌سازی**: بر اساس سن و سبک یادگیری
- ✅ **اهداف مشخص**: کاربر می‌داند چرا زبان یاد می‌گیرد
- ✅ **تجربه بهتر**: معلم می‌تواند بهتر برنامه‌ریزی کند
- ✅ **نه خیلی ساده**: اطلاعات کافی برای کار
- ✅ **نه خیلی پیچیده**: کاربر گیج نمی‌شود

## 📊 **ساختار فرم:**

```
┌─────────────────────────────────────┐
│           اطلاعات شخصی              │
├─────────────────────────────────────┤
│ نام | نام خانوادگی | تلفن | سن     │
├─────────────────────────────────────┤
│           سطح زبان                  │
├─────────────────────────────────────┤
│        سطح زبان فعلی                │
├─────────────────────────────────────┤
│         زبان‌های انتخابی            │
├─────────────────────────────────────┤
│        اهداف یادگیری                │
├─────────────────────────────────────┤
│         سبک یادگیری                 │
└─────────────────────────────────────┘
```

## 🔍 **نتیجه نهایی:**

پس از اجرای این تغییرات:
- ✅ فرم دانش‌آموز کامل و کاربردی شده
- ✅ اطلاعات ضروری برای پروفایل جمع‌آوری می‌شود
- ✅ دیتابیس با فرم کاملاً مطابقت دارد
- ✅ تجربه کاربری بهینه شده
- ✅ معلم اطلاعات کافی برای برنامه‌ریزی دارد
