# 🚀 راه‌حل سریع دسترسی ادمین به معلمان

## 🚨 مشکل
جدول `teachers` در Supabase وجود دارد اما ادمین نمی‌تواند به آن دسترسی داشته باشد.

## ✅ راه‌حل در 3 مرحله

### مرحله 1: اجرای اسکریپت اصلی
فایل `database/fix_admin_access_to_existing_teachers.sql` را در Supabase اجرا کنید:

1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. روی "SQL Editor" کلیک کنید
4. محتوای فایل `fix_admin_access_to_existing_teachers.sql` را کپی کنید
5. روی "Run" کلیک کنید

### مرحله 2: اضافه کردن کاربر به عنوان ادمین
فایل `database/add_current_user_as_admin_simple.sql` را اجرا کنید:

1. در همان SQL Editor
2. محتوای فایل `add_current_user_as_admin_simple.sql` را کپی کنید
3. **مهم**: ایمیل خود را در دو جا جایگزین کنید:
   ```sql
   WHERE email = 'your-email@example.com'  -- ایمیل خود را اینجا قرار دهید
   ```
4. روی "Run" کلیک کنید

### مرحله 3: تست
1. صفحه `/admin/teachers` را refresh کنید
2. باید لیست معلمان نمایش داده شود

## 🔍 بررسی موفقیت

پس از اجرای هر دو اسکریپت، باید پیام‌های زیر را ببینید:
```
✅ Admin teachers access setup completed successfully!
✅ User added as admin successfully!
```

## 🧪 تست نهایی

```sql
-- این query باید کار کند
SELECT COUNT(*) FROM public.teachers;
```

## 📞 در صورت مشکل

### بررسی console مرورگر:
1. F12 → Console
2. خطاها را بررسی کنید

### بررسی Supabase:
1. Authentication → Policies
2. جدول `teachers` را انتخاب کنید
3. مطمئن شوید که RLS فعال است

### خطاهای رایج:
- **"permission denied"** → اسکریپت‌ها را دوباره اجرا کنید
- **"relation does not exist"** → جدول وجود ندارد
- **"column does not exist"** → ساختار جدول تغییر کرده

## 🎯 نتیجه
پس از اجرای این مراحل، داشبورد ادمین باید کار کند و شما بتوانید تمام معلمان را ببینید.
