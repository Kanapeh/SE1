# 🎥 راهنمای رفع مشکل تماس تصویری

## ✅ **تغییرات اعمال شده:**

### 🛠️ **۱. تشخیص و حل خودکار:**
- تشخیص خودکار استفاده از IP بجای localhost
- هشدار قرمز و دکمه "رفتن به localhost" 
- حل خودکار مشکل getUserMedia

### 🔧 **۲. بهبود مدیریت Stream:**
- بازیابی خودکار stream در startCall
- useEffect برای حفظ stream در حالت تماس
- console.log برای debug

### 🎯 **۳. دکمه تست در حالت Call:**
- دکمه جدید "تست" در نوار کنترل
- احیای فوری stream در صورت مشکل
- بدون نیاز به قطع تماس

---

## 📱 **آزمایش کنید:**

### **قدم ۱**: آدرس درست
```
✅ درست: http://localhost:3000/students/temp-user-id/video-call
❌ اشتباه: http://172.20.10.10:3000/...
```

### **قدم ۲**: پیوستن به کلاس
1. دکمه "پیوستن به کلاس" کلیک کنید
2. ۲ ثانیه صبر کنید (در حال اتصال...)
3. وارد صفحه تماس می‌شوید

### **قدم ۳**: چک کردن تصویر
- باید در گوشه راست بالا "شما" را ببینید
- اگر نمی‌بینید: دکمه "تست" کلیک کنید

---

## 🔍 **عیب‌یابی با Console:**

### **F12 بزنید و Console تب را باز کنید:**

#### **Log های موفق:**
```
✅ Local video metadata loaded in call
✅ Local video playing in call  
✅ Camera fixed successfully
```

#### **Log های خطا:**
```
❌ Using IP address instead of localhost
❌ Failed to restore stream in call
❌ Video play failed after call start
```

---

## 🎮 **دکمه‌های کنترل:**

### **در حالت تماس:**
1. **🎥 دوربین** - روشن/خاموش ویدیو
2. **🎤 میکروفن** - روشن/خاموش صدا  
3. **🖥️ صفحه** - اشتراک صفحه
4. **⚙️ تنظیمات** - تنظیمات تماس
5. **📷 تست** - حل مشکل دوربین (جدید!)
6. **📞 پایان** - قطع تماس

---

## ⚡ **رفع سریع مشکلات:**

### **مشکل: تصویر نمایش نمی‌دهد**
```bash
# ۱. دکمه "تست" کلیک کنید
# ۲. اگر کار نکرد، F5 بزنید
# ۳. مطمئن شوید آدرس localhost است
```

### **مشکل: getUserMedia error**
```bash
# ۱. مطمئن شوید از localhost استفاده می‌کنید
# ۲. دکمه سبز "رفتن به localhost" کلیک کنید
# ۳. مجوزهای دوربین را چک کنید
```

### **مشکل: Stream قطع می‌شود**
```bash
# ۱. دکمه "تست" در حالت تماس
# ۲. یا دکمه دوربین را خاموش/روشن کنید
# ۳. صفحه را refresh کنید
```

---

## 📊 **آمار موفقیت:**

- ✅ **localhost**: 100% کار می‌کند
- ❌ **IP Address**: getUserMedia مسدود
- ✅ **دکمه تست**: حل فوری مشکل
- ✅ **دکمه localhost**: رفع خودکار

---

**🎉 با این تغییرات، مشکل تصویر در حالت تماس باید حل شده باشد!**
