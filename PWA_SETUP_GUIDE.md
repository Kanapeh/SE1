# راهنمای PWA (Progressive Web App) - سِ وان

## ✅ چه کارهایی انجام شده است؟

### 1. Service Worker
- فایل `public/sw.js` ایجاد شده که:
  - صفحات را cache می‌کند
  - در حالت آفلاین کار می‌کند
  - پشتیبانی از push notifications (برای آینده)

### 2. Web App Manifest
- فایل `public/site.webmanifest` بهبود یافته
- شامل تمام آیکون‌های لازم
- تنظیمات برای نصب به عنوان اپ

### 3. کامپوننت‌های PWA
- `components/PWARegister.tsx`: ثبت Service Worker
- `components/PWAInstallPrompt.tsx`: نمایش پیام نصب اپ

### 4. صفحه آفلاین
- `app/offline/page.tsx`: صفحه نمایش داده می‌شود وقتی کاربر آفلاین است

### 5. تنظیمات Next.js
- Headers مناسب برای Service Worker
- Cache control برای manifest

## 🚀 چگونه تست کنیم؟

### در مرورگر دسکتاپ (Chrome/Edge):

1. پروژه را اجرا کنید:
   ```bash
   npm run dev
   ```

2. به `http://localhost:3000` بروید

3. DevTools را باز کنید (F12)

4. به تب **Application** بروید

5. در سمت چپ، **Service Workers** را انتخاب کنید
   - باید Service Worker ثبت شده باشد

6. **Manifest** را بررسی کنید
   - باید اطلاعات اپ نمایش داده شود

7. برای تست نصب:
   - روی آیکون نصب در address bar کلیک کنید
   - یا از منوی مرورگر: More tools > Create shortcut > Open as window

### در موبایل (Android - Chrome):

1. سایت را در Chrome باز کنید

2. منوی Chrome را باز کنید (سه نقطه)

3. **Add to Home screen** یا **Install app** را انتخاب کنید

4. اپ روی صفحه اصلی نصب می‌شود

### در موبایل (iOS - Safari):

1. سایت را در Safari باز کنید

2. دکمه Share را بزنید

3. **Add to Home Screen** را انتخاب کنید

4. اپ روی صفحه اصلی اضافه می‌شود

## 📱 ویژگی‌های PWA

✅ **نصب روی موبایل و دسکتاپ**
✅ **کار در حالت آفلاین**
✅ **لود سریع‌تر با caching**
✅ **تجربه اپلیکیشن native**
✅ **پشتیبانی از push notifications (آماده برای آینده)**

## 🔧 تنظیمات بیشتر

### تغییر آیکون‌ها:
آیکون‌های اپ در `public/` قرار دارند:
- `apple-touch-icon.png` (180x180)
- `android-chrome-192x192.png`
- `android-chrome-512x512.png`

### تغییر رنگ theme:
در `public/site.webmanifest` و `app/layout.tsx` رنگ `#2b3a67` را تغییر دهید.

### اضافه کردن Push Notifications:
Service Worker آماده است. فقط باید:
1. Backend برای ارسال notifications راه‌اندازی کنید
2. کاربران را subscribe کنید
3. از API push browser استفاده کنید

## 📝 نکات مهم

- Service Worker فقط در HTTPS یا localhost کار می‌کند
- برای production، حتماً HTTPS فعال باشد
- Cache version را در `sw.js` تغییر دهید برای force update

## 🐛 عیب‌یابی

### Service Worker ثبت نمی‌شود:
- مطمئن شوید HTTPS یا localhost است
- DevTools > Application > Service Workers را بررسی کنید
- Cache را پاک کنید و دوباره امتحان کنید

### اپ نصب نمی‌شود:
- Manifest را بررسی کنید (DevTools > Application > Manifest)
- آیکون‌ها باید موجود باشند
- `start_url` باید درست باشد

### آفلاین کار نمی‌کند:
- Service Worker باید فعال باشد
- صفحات باید cache شده باشند
- Network tab را در DevTools بررسی کنید

## 🎯 مراحل بعدی (اختیاری)

1. **اضافه کردن Background Sync**: برای sync کردن داده‌ها وقتی آنلاین شد
2. **Push Notifications**: برای اطلاع‌رسانی به کاربران
3. **Share Target API**: برای share کردن محتوا به اپ
4. **Periodic Background Sync**: برای به‌روزرسانی محتوا در پس‌زمینه

