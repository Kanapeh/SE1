# راهنمای قابلیت‌های پیشرفته مقالات

## مراحل راه‌اندازی

### 1. به‌روزرسانی جدول مقالات

فایل `database/update_blog_posts_table.sql` را در Supabase SQL Editor اجرا کنید تا فیلدهای جدید اضافه شوند.

**نکته:** جدول `blog_posts` شما از قبل وجود دارد و فقط نیاز به اضافه کردن فیلدهای جدید دارد.

### 2. نصب وابستگی‌ها

```bash
npm install chart.js react-chartjs-2
```

## ساختار جدول نهایی

پس از اجرای اسکریپت، جدول شما شامل این فیلدهای جدید خواهد بود:

```sql
-- فیلدهای جدید اضافه شده
video_url text NULL,           -- آدرس ویدیو
chart_data jsonb NULL,         -- داده‌های نمودار (JSON)
table_data jsonb NULL,         -- داده‌های جدول (JSON)
has_video boolean DEFAULT FALSE,  -- آیا شامل ویدیو است
has_chart boolean DEFAULT FALSE,  -- آیا شامل نمودار است
has_table boolean DEFAULT FALSE   -- آیا شامل جدول است
```

## قابلیت‌های جدید

### 🎥 ویدیو
- پشتیبانی از YouTube و Vimeo
- پشتیبانی از فایل‌های مستقیم (MP4, WebM, OGG)
- نمایش خودکار در مقالات

### 📊 نمودار
- پشتیبانی از Chart.js
- انواع نمودار: خطی، ستونی، دایره‌ای، دونات
- داده‌های JSON قابل تنظیم

### 📋 جدول
- جدول‌های تعاملی
- قابلیت جستجو و مرتب‌سازی
- صفحه‌بندی خودکار

## نحوه استفاده

### برای ادمین:

#### 1. افزودن ویدیو:
1. به پنل مدیریت بروید (`/admin`)
2. روی "مدیریت وبلاگ" کلیک کنید
3. مقاله جدید ایجاد کنید یا مقاله موجود را ویرایش کنید
4. گزینه "شامل ویدیو" را تیک بزنید
5. آدرس ویدیو را وارد کنید:
   - YouTube: `https://www.youtube.com/watch?v=VIDEO_ID`
   - Vimeo: `https://vimeo.com/VIDEO_ID`
   - فایل مستقیم: `https://example.com/video.mp4`

#### 2. افزودن نمودار:
1. گزینه "شامل نمودار" را تیک بزنید
2. داده‌های JSON نمودار را وارد کنید:

```json
{
  "type": "bar",
  "data": {
    "labels": ["فروردین", "اردیبهشت", "خرداد"],
    "datasets": [
      {
        "label": "فروش",
        "data": [65, 59, 80],
        "backgroundColor": "rgba(54, 162, 235, 0.2)",
        "borderColor": "rgba(54, 162, 235, 1)",
        "borderWidth": 1
      }
    ]
  }
}
```

#### 3. افزودن جدول:
1. گزینه "شامل جدول" را تیک بزنید
2. داده‌های JSON جدول را وارد کنید:

```json
{
  "headers": ["نام", "سن", "شهر"],
  "rows": [
    ["علی", 25, "تهران"],
    ["فاطمه", 30, "اصفهان"],
    ["محمد", 28, "مشهد"]
  ]
}
```

### برای کاربران:

#### مشاهده محتوا:
- ویدیوها، نمودارها و جداول به صورت خودکار در مقالات نمایش داده می‌شوند
- جداول قابل جستجو و مرتب‌سازی هستند
- نمودارها تعاملی و قابل زوم هستند

## مثال‌های JSON

### نمودار خطی:
```json
{
  "type": "line",
  "data": {
    "labels": ["هفته 1", "هفته 2", "هفته 3", "هفته 4"],
    "datasets": [
      {
        "label": "پیشرفت",
        "data": [10, 20, 35, 50],
        "borderColor": "rgb(75, 192, 192)",
        "backgroundColor": "rgba(75, 192, 192, 0.2)"
      }
    ]
  }
}
```

### نمودار دایره‌ای:
```json
{
  "type": "pie",
  "data": {
    "labels": ["قرمز", "آبی", "زرد"],
    "datasets": [
      {
        "data": [300, 50, 100],
        "backgroundColor": [
          "rgb(255, 99, 132)",
          "rgb(54, 162, 235)",
          "rgb(255, 205, 86)"
        ]
      }
    ]
  }
}
```

### جدول پیچیده:
```json
{
  "headers": ["نام محصول", "قیمت", "موجودی", "دسته‌بندی"],
  "rows": [
    ["لپ‌تاپ", "15,000,000", "10", "الکترونیک"],
    ["موبایل", "8,000,000", "25", "الکترونیک"],
    ["کتاب", "150,000", "100", "آموزشی"],
    ["میز", "2,500,000", "5", "مبل"]
  ]
}
```

## فایل‌های SQL

### 1. `database/update_blog_posts_table.sql`
اسکریپت برای اضافه کردن فیلدهای جدید به جدول موجود

### 2. `database/blog_posts_complete_structure.sql`
ساختار کامل جدول پس از به‌روزرسانی (برای مرجع)

## نکات مهم

### امنیت:
- فقط URL های معتبر YouTube و Vimeo پذیرفته می‌شوند
- داده‌های JSON اعتبارسنجی می‌شوند
- محتوا از طریق CSP محافظت می‌شود

### عملکرد:
- ویدیوها به صورت lazy load بارگذاری می‌شوند
- نمودارها فقط در صورت نیاز رندر می‌شوند
- جداول برای داده‌های بزرگ بهینه شده‌اند

### سازگاری:
- پشتیبانی کامل از RTL
- سازگار با موبایل
- پشتیبانی از تم‌های مختلف

## عیب‌یابی

### مشکلات رایج:

1. **نمودار نمایش داده نمی‌شود:**
   - JSON را بررسی کنید
   - از فرمت صحیح استفاده کنید
   - console را برای خطاها بررسی کنید

2. **ویدیو پخش نمی‌شود:**
   - آدرس URL را بررسی کنید
   - از URL های embed استفاده کنید
   - دسترسی ویدیو را بررسی کنید

3. **جدول خالی است:**
   - ساختار JSON را بررسی کنید
   - headers و rows را درست تعریف کنید

## پشتیبانی

برای سوالات و مشکلات:
- مستندات Chart.js: https://www.chartjs.org/
- مستندات YouTube Embed: https://developers.google.com/youtube/iframe_api_reference
- مستندات Vimeo Embed: https://developer.vimeo.com/player/sdk/embed 