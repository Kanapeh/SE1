# 🎯 راهنمای ایجاد جداول مطابق با کد پروژه

## ✅ **مشکل حل شد!**

جدول‌های دیتابیس حالا **دقیقاً مطابق با کد پروژه** ایجاد می‌شوند.

## 🔧 **فایل SQL جدید:**

### **`database/create_tables_for_code.sql`** - **توصیه شده**
- ✅ تمام فیلدهای اجباری `NOT NULL` هستند
- ✅ ساختار دقیقاً مطابق با interface های TypeScript
- ✅ RLS policies و index ها فعال شده‌اند

## 📋 **ساختار جداول:**

### **جدول `teachers`:**
```sql
-- فیلدهای اجباری (NOT NULL)
id, email, first_name, last_name, phone, gender, birthdate, 
national_id, address, languages, levels, class_types, 
available_days, available_hours, max_students_per_class, 
bio, experience_years, hourly_rate, location, education, 
certificates, teaching_methods, achievements, avatar, preferred_time

-- فیلدهای اختیاری
status (default: 'pending'), available (default: true)
```

### **جدول `students`:**
```sql
-- فیلدهای اجباری (NOT NULL)
id, email, first_name, last_name, phone, gender, birthdate, 
address, education_level, learning_goals, preferred_languages, 
preferred_learning_style, availability, notes, avatar

-- فیلدهای اختیاری
status (default: 'active')
```

## 🚀 **مراحل اجرا:**

### **مرحله 1: اجرای فایل SQL**
1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. روی "SQL Editor" کلیک کنید
4. محتوای فایل `database/create_tables_for_code.sql` را کپی کنید
5. روی "Run" کلیک کنید

### **مرحله 2: بررسی موفقیت**
پس از اجرا، باید پیام زیر را ببینید:
```
Tables created successfully for code compatibility!
```

### **مرحله 3: تست سیستم**
1. سرور را restart کنید
2. به صفحه `/complete-profile` بروید
3. خطاها باید برطرف شده باشند

## 🔍 **مقایسه با جدول قبلی:**

### ❌ **جدول قبلی (مشکل‌دار):**
- فیلدهای اجباری `nullable` بودند
- ساختار با کد مطابقت نداشت
- خطای "column does not exist" رخ می‌داد

### ✅ **جدول جدید (مطابق با کد):**
- تمام فیلدهای اجباری `NOT NULL` هستند
- ساختار دقیقاً مطابق با interface ها
- کد بدون خطا کار می‌کند

## 🎯 **نتیجه نهایی:**

پس از اجرای این فایل:
- ✅ جداول مطابق با کد ایجاد می‌شوند
- ✅ صفحه `/complete-profile` بدون خطا کار می‌کند
- ✅ فرم‌های ثبت‌نام کار می‌کنند
- ✅ سیستم آماده استفاده است

## 📞 **در صورت مشکل:**

### خطاهای احتمالی:
1. **"relation already exists"**
   - جدول قبلاً وجود دارد، نادیده بگیرید

2. **"permission denied"**
   - مطمئن شوید که در Supabase هستید

3. **"syntax error"**
   - فایل را دوباره کپی کنید

### تماس با پشتیبانی:
- Console مرورگر را بررسی کنید
- Supabase logs را چک کنید
- Error message دقیق را یادداشت کنید
