# راهنمای استفاده از قابلیت‌های جدید داشبورد دانشجو

## 🎯 قابلیت‌های جدید اضافه شده

### 1. کلیک روی کلاس‌های رزرو شده
- **قبل**: فقط نمایش اطلاعات کلاس
- **حالا**: کلیک روی هر کلاس مستقیماً به صفحه تماس تصویری هدایت می‌کند

### 2. دکمه‌های عملیات سریع
هر کلاس حالا دارای دکمه‌های عملیاتی است:
- **شروع کلاس**: برای کلاس‌های تایید شده
- **جزئیات**: مشاهده اطلاعات کامل کلاس
- **تغییر زمان**: برای کلاس‌های قابل تغییر
- **لغو**: برای کلاس‌های قابل لغو

### 3. Modal جزئیات کلاس
با کلیک روی دکمه "جزئیات":
- نمایش تمام اطلاعات کلاس
- امکان شروع کلاس از داخل modal
- دکمه‌های عملیاتی مختلف

## 🚀 نحوه استفاده

### مرحله 1: ورود به داشبورد
```
http://localhost:3000/dashboard/student
```

### مرحله 2: رفتن به تب "کلاس‌ها"
- روی تب "کلاس‌ها" کلیک کنید
- لیست کلاس‌های رزرو شده نمایش داده می‌شود

### مرحله 3: انتخاب کلاس
**روش 1: کلیک مستقیم**
- روی هر کلاس کلیک کنید
- مستقیماً به صفحه تماس تصویری هدایت می‌شوید

**روش 2: دکمه شروع کلاس**
- روی دکمه "شروع کلاس" کلیک کنید
- برای کلاس‌های تایید شده فعال است

**روش 3: دکمه جزئیات**
- روی دکمه "جزئیات" کلیک کنید
- modal جزئیات باز می‌شود
- از داخل modal می‌توانید کلاس را شروع کنید

## 📱 صفحه تماس تصویری

### قابلیت‌های موجود:
1. **دوربین و میکروفن**: کنترل کامل
2. **چت**: ارسال پیام متنی
3. **وایت‌بورد**: رسم و نوشتن
4. **اشتراک‌گذاری صفحه**: نمایش صفحه کامپیوتر
5. **کنترل‌های موبایل**: بهینه‌سازی برای موبایل

### کنترل‌های اصلی:
- **دوربین**: روشن/خاموش کردن
- **میکروفن**: روشن/خاموش کردن
- **اشتراک صفحه**: شروع/توقف
- **چت**: باز/بسته کردن
- **وایت‌بورد**: باز/بسته کردن
- **پایان تماس**: خروج از کلاس

## 🔧 عیب‌یابی

### مشکل 1: دوربین کار نمی‌کند
**راه‌حل‌ها:**
- از localhost استفاده کنید (نه IP)
- دسترسی دوربین را فعال کنید
- مرورگر را refresh کنید
- از دکمه "تست دوربین" استفاده کنید

### مشکل 2: کلاس شروع نمی‌شود
**راه‌حل‌ها:**
- وضعیت کلاس را بررسی کنید
- فقط کلاس‌های تایید شده قابل شروع هستند
- اتصال اینترنت را بررسی کنید

### مشکل 3: Modal جزئیات باز نمی‌شود
**راه‌حل‌ها:**
- JavaScript را فعال کنید
- مرورگر را refresh کنید
- از تب "کلاس‌ها" استفاده کنید

## 📋 وضعیت‌های کلاس

### 1. **در انتظار تایید** (pending)
- دکمه‌های فعال: جزئیات، لغو
- دکمه شروع کلاس: غیرفعال

### 2. **تایید شده** (confirmed)
- دکمه‌های فعال: شروع کلاس، جزئیات، تغییر زمان، لغو
- آماده برای شروع کلاس

### 3. **برنامه‌ریزی شده** (scheduled)
- دکمه‌های فعال: شروع کلاس، جزئیات، تغییر زمان، لغو
- آماده برای شروع کلاس

### 4. **تکمیل شده** (completed)
- دکمه‌های فعال: جزئیات
- دکمه شروع کلاس: غیرفعال

## 🎨 ویژگی‌های UI/UX

### 1. **انیمیشن‌ها**
- ورود و خروج smooth
- Hover effects
- Loading states

### 2. **Responsive Design**
- بهینه‌سازی برای موبایل
- کنترل‌های عمودی در موبایل
- کنترل‌های افقی در دسکتاپ

### 3. **رنگ‌بندی هوشمند**
- سبز: عملیات مثبت
- قرمز: عملیات منفی
- آبی: اطلاعات
- زرد: هشدار

## 🔒 امنیت

### 1. **کنترل دسترسی**
- فقط دانشجویان ثبت‌نام شده
- بررسی وضعیت کلاس
- محدودیت زمانی

### 2. **محافظت از محتوا**
- جلوگیری از ضبط صفحه
- محافظت از کلیدهای میانبر
- غیرفعال کردن developer tools

## 📱 بهینه‌سازی موبایل

### 1. **کنترل‌های عمودی**
- دکمه‌ها در سمت چپ
- اندازه مناسب برای لمس
- فاصله مناسب بین دکمه‌ها

### 2. **نمایش محتوا**
- ویدیو تمام صفحه
- چت overlay
- وایت‌بورد responsive

## 🚀 نکات مهم

### 1. **قبل از شروع کلاس**
- دوربین و میکروفن را تست کنید
- اتصال اینترنت را بررسی کنید
- محیط مناسب را انتخاب کنید

### 2. **در حین کلاس**
- از دکمه‌های کنترل استفاده کنید
- در صورت مشکل، از دکمه "تست دوربین" استفاده کنید
- از چت برای ارتباط متنی استفاده کنید

### 3. **پس از کلاس**
- کلاس را به درستی پایان دهید
- بازخورد ارائه دهید
- یادداشت‌ها را بررسی کنید

## 📞 پشتیبانی

در صورت مشکل:
1. Console errors را بررسی کنید
2. Network tab را چک کنید
3. مرورگر را refresh کنید
4. با تیم پشتیبانی تماس بگیرید

---

**نکته مهم**: این قابلیت‌ها برای بهبود تجربه یادگیری طراحی شده‌اند. لطفاً از آنها به درستی استفاده کنید.
