# 🚀 راه‌حل سریع مشکل دانش‌آموز فعلی

## 🚨 مشکل
صفحه video-call اطلاعات دانش‌آموز نمونه را نمایش می‌دهد، نه اطلاعات دانش‌آموز فعلی که لاگین کرده.

## ✅ راه‌حل در 2 مرحله

### مرحله 1: اضافه کردن کاربر فعلی به عنوان دانش‌آموز
فایل `database/add_current_student.sql` را در Supabase اجرا کنید:

1. به Supabase Dashboard بروید
2. SQL Editor را باز کنید
3. محتوای `add_current_student.sql` را کپی کنید
4. **مهم**: ایمیل خود را در دو جا جایگزین کنید:
   ```sql
   WHERE email = 'your-email@example.com'  -- ایمیل خود را اینجا قرار دهید
   ```
5. اجرا کنید

**نتیجه مورد انتظار:**
```
✅ Current user added as student successfully!
```

### مرحله 2: تست سیستم
1. صفحه video-call را refresh کنید
2. باید اطلاعات دانش‌آموز فعلی شما نمایش داده شود

## 🔍 آنچه اصلاح شده

### 1. **صفحه video-call معلم**:
- ✅ اطلاعات دانش‌آموز فعلی که لاگین کرده خوانده می‌شود
- ✅ اطلاعات واقعی از جدول `students` خوانده می‌شود
- ✅ سطح و زبان واقعی دانش‌آموز نمایش داده می‌شود

### 2. **جدول students**:
- ✅ کاربر فعلی به عنوان دانش‌آموز اضافه شده
- ✅ اطلاعات واقعی (نام، ایمیل، سطح، زبان) ذخیره شده

### 3. **جدول classes**:
- ✅ کلاس نمونه برای معلم سپنتا و دانش‌آموز فعلی ایجاد شده

## 🧪 تست نهایی

پس از اجرای اسکریپت، این query باید کار کند:
```sql
SELECT 
    s.first_name,
    s.last_name,
    s.email,
    s.education_level,
    s.preferred_languages
FROM public.students s
WHERE s.email = 'your-email@example.com';
```

## 📊 اطلاعات نمایش داده شده

پس از اصلاح، باید ببینید:
- **نام**: نام واقعی شما (نه "دانش‌آموز فعلی")
- **ایمیل**: ایمیل واقعی شما (نه "student@example.com")
- **سطح**: سطح واقعی شما (از جدول students)
- **زبان**: زبان‌های انتخابی واقعی شما

## 🎯 نتیجه
حالا صفحه video-call اطلاعات دانش‌آموز فعلی شما را نمایش می‌دهد!

## 📞 در صورت مشکل
اگر هنوز اطلاعات درست نمایش داده نمی‌شود:
1. Console مرورگر را بررسی کنید
2. مطمئن شوید که ایمیل درست جایگزین شده
3. جدول students را بررسی کنید
