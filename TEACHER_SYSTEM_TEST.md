# 🧪 راهنمای تست سیستم ثبت‌نام معلمان

## ✅ **وضعیت فعلی:**
- ✅ جدول `teachers` در Supabase وجود دارد
- ✅ کد فرم با ساختار جدول تطبیق داده شده
- ✅ فیلدهای اختیاری و اجباری مشخص شده

## 🚀 **تست سریع سیستم:**

### **1. دسترسی به فرم:**
```
/register?type=teacher -> دکمه "ثبت‌نام کامل معلم"
/register/teacher -> فرم 5 مرحله‌ای
```

### **2. تست فرم مرحله به مرحله:**

#### **مرحله 1: اطلاعات پایه**
- ✅ نام و نام خانوادگی (اجباری)
- ✅ ایمیل (اجباری)
- ✅ رمز عبور (اجباری)
- ✅ شماره تلفن (اجباری)
- ✅ جنسیت (اجباری)
- ✅ تاریخ تولد (اجباری)
- ✅ کد ملی (اختیاری)

#### **مرحله 2: پروفایل تدریس**
- ✅ زبان‌های تدریس (اجباری)
- ✅ سطح‌های تدریس (اختیاری)
- ✅ نوع کلاس (اجباری)
- ✅ سال‌های تجربه (اجباری)
- ✅ تحصیلات (اجباری)
- ✅ بیوگرافی (اجباری)

#### **مرحله 3: زمان‌بندی و قیمت**
- ✅ روزهای کاری (اجباری)
- ✅ ساعات کاری (اجباری)
- ✅ نرخ ساعتی (اختیاری)
- ✅ حداکثر دانش‌آموز (اختیاری)
- ✅ محل سکونت (اجباری)

#### **مرحله 4: مدارک و روش‌ها**
- ✅ روش‌های تدریس (اجباری)
- ✅ گواهینامه‌ها (اختیاری)
- ✅ دستاوردها (اختیاری)

#### **مرحله 5: تایید نهایی**
- ✅ آدرس (اختیاری)
- ✅ توضیحات (اختیاری)
- ✅ موافقت با قوانین (اجباری)

## 🔧 **تست عملکرد:**

### **1. تست validation:**
- هر مرحله را بدون تکمیل فیلدهای اجباری امتحان کنید
- باید پیام خطا نمایش داده شود

### **2. تست navigation:**
- بین مراحل حرکت کنید
- دکمه‌های قبلی/بعدی کار کنند

### **3. تست ذخیره:**
- فرم را کامل کنید
- داده‌ها در دیتابیس ذخیره شوند

## 📊 **بررسی دیتابیس:**

### **1. بررسی جدول:**
```sql
-- بررسی ساختار جدول
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns 
WHERE table_name = 'teachers' 
ORDER BY ordinal_position;

-- بررسی داده‌های ثبت شده
SELECT * FROM teachers ORDER BY created_at DESC LIMIT 5;
```

### **2. بررسی RLS:**
```sql
-- بررسی policies
SELECT * FROM pg_policies WHERE tablename = 'teachers';

-- تست دسترسی
SELECT * FROM teachers WHERE status = 'active';
```

## 🚨 **مشکلات احتمالی:**

### **1. خطای "relation teachers does not exist":**
- جدول در schema دیگری است
- نام جدول اشتباه است

### **2. خطای "permission denied":**
- RLS policies فعال نیست
- User authentication مشکل دارد

### **3. خطای "column does not exist":**
- نام فیلد اشتباه است
- ساختار جدول تغییر کرده

## 🎯 **نکات مهم:**

### **1. فیلدهای اجباری:**
- `first_name`, `last_name`, `email`
- `languages`, `class_types`
- `experience_years`, `education`, `bio`
- `available_days`, `available_hours`
- `location`, `teaching_methods`

### **2. فیلدهای اختیاری:**
- `national_id`, `hourly_rate`
- `levels`, `certificates`, `achievements`
- `address`, `notes`

### **3. مقادیر پیش‌فرض:**
- `status`: 'pending'
- `available`: true
- `created_at`: now()
- `updated_at`: now()

## 📋 **چک‌لیست تست:**

- [ ] فرم مرحله 1 باز می‌شود
- [ ] validation مرحله 1 کار می‌کند
- [ ] انتقال به مرحله 2
- [ ] validation مرحله 2 کار می‌کند
- [ ] انتقال به مرحله 3
- [ ] validation مرحله 3 کار می‌کند
- [ ] انتقال به مرحله 4
- [ ] validation مرحله 4 کار می‌کند
- [ ] انتقال به مرحله 5
- [ ] validation مرحله 5 کار می‌کند
- [ ] فرم submit می‌شود
- [ ] داده‌ها در دیتابیس ذخیره می‌شود
- [ ] redirect به صفحه مناسب

## 🎉 **در صورت موفقیت:**

اگر همه چیز درست کار کرد:
- ✅ سیستم ثبت‌نام معلمان آماده است
- ✅ فرم چند مرحله‌ای کار می‌کند
- ✅ داده‌ها در دیتابیس ذخیره می‌شود
- ✅ امنیت با RLS تامین می‌شود

## 📞 **در صورت مشکل:**

1. **Console مرورگر** را بررسی کنید
2. **Network tab** را چک کنید
3. **Supabase logs** را بررسی کنید
4. **Error message** دقیق را یادداشت کنید

**سیستم آماده تست است! 🚀**
