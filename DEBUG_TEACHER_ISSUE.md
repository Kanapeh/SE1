# 🔍 عیب‌یابی مشکل معلم اشتباه

## 🚨 مشکل
وقتی روی "شروع کلاس آنلاین" کلیک می‌کنید، اطلاعات معلم اشتباه (استاد احمدی) نمایش داده می‌شود، در حالی که شما با سپنتا علیزاده کلاس برداشته‌اید.

## 🔍 مراحل عیب‌یابی

### مرحله 1: بررسی console مرورگر
1. F12 را فشار دهید
2. به تب Console بروید
3. صفحه video-call را refresh کنید
4. این log ها را بررسی کنید:
   - `Teacher ID from URL: ...`
   - `Teacher query result: ...`
   - `Setting teacher data: ...`

### مرحله 2: بررسی اطلاعات معلم در دیتابیس
فایل `database/check_teacher_data.sql` را در Supabase اجرا کنید:

1. به Supabase Dashboard بروید
2. SQL Editor را باز کنید
3. محتوای `check_teacher_data.sql` را کپی کنید
4. اجرا کنید

### مرحله 3: بررسی نتایج

#### نتایج مورد انتظار:
- **معلم سپنتا علیزاده** باید در جدول `teachers` وجود داشته باشد
- **ID معلم سپنتا** باید با ID در URL مطابقت داشته باشد
- **کلاس‌های معلم سپنتا** باید در جدول `classes` وجود داشته باشند

#### مشکلات احتمالی:

##### مشکل 1: ID اشتباه در URL
```
URL: /teachers/5b60e402-ebc9-4424-bc28-a79b95853cd2/video-call
```
این ID ممکن است متعلق به معلم دیگری باشد.

##### مشکل 2: معلم سپنتا در دیتابیس نیست
اگر معلم سپنتا در جدول `teachers` وجود ندارد، باید اضافه شود.

##### مشکل 3: کلاس اشتباه انتخاب شده
کلاس ممکن است متعلق به معلم دیگری باشد.

## ✅ راه‌حل‌های احتمالی

### راه‌حل 1: بررسی ID معلم سپنتا
```sql
-- پیدا کردن ID معلم سپنتا
SELECT id, first_name, last_name, email
FROM public.teachers 
WHERE first_name = 'سپنتا' AND last_name = 'علیزاده';
```

### راه‌حل 2: ایجاد معلم سپنتا (اگر وجود ندارد)
```sql
-- اضافه کردن معلم سپنتا
INSERT INTO public.teachers (
    id,
    email,
    first_name,
    last_name,
    phone,
    languages,
    teaching_methods,
    status,
    available
) VALUES (
    gen_random_uuid(),
    'sepanta.alizadeh@example.com',
    'سپنتا',
    'علیزاده',
    '09123456789',
    ARRAY['انگلیسی'],
    ARRAY['مکالمه'],
    'active',
    true
);
```

### راه‌حل 3: اصلاح کلاس موجود
```sql
-- بررسی کلاس‌های موجود
SELECT * FROM public.classes 
WHERE teacher_id = '5b60e402-ebc9-4424-bc28-a79b95853cd2';

-- اگر کلاس اشتباه است، آن را حذف کنید
DELETE FROM public.classes 
WHERE id = 'class-id-here';
```

## 🧪 تست نهایی

### تست 1: بررسی URL صحیح
مطمئن شوید که به URL صحیح معلم سپنتا می‌روید:
```
/teachers/{correct-teacher-id}/video-call
```

### تست 2: بررسی console logs
Console باید این اطلاعات را نشان دهد:
```
Teacher ID from URL: [correct-id]
Setting teacher data: { first_name: 'سپنتا', last_name: 'علیزاده', ... }
```

### تست 3: بررسی صفحه
صفحه باید اطلاعات معلم سپنتا را نمایش دهد، نه استاد احمدی.

## 📞 اطلاعات مورد نیاز

### برای عیب‌یابی بهتر:
1. **Console logs کامل** از مرورگر
2. **نتایج `check_teacher_data.sql`** از Supabase
3. **URL دقیق** که به آن می‌روید
4. **نام معلمی** که انتظار دارید ببینید

### تماس با پشتیبانی:
تمام اطلاعات بالا را جمع‌آوری کنید و به من اطلاع دهید.

## 🎯 نتیجه مورد انتظار

پس از حل مشکل:
- ✅ صفحه video-call اطلاعات معلم سپنتا علیزاده را نمایش می‌دهد
- ✅ اطلاعات معلم اشتباه (استاد احمدی) نمایش داده نمی‌شود
- ✅ کلاس‌های صحیح نمایش داده می‌شوند
